/**
 * @param {string} s
 * @return {number}
 */

var map = {
    'I': 1,
    'V': 5,
    'X': 10,
    'L': 50,
    'C': 100,
    'D': 500,
    'M': 1000
};

var romanToInt = function(s) {
    
    for (var i = 0, l = s.length; i < l; i ++) {
        var c = s[i];
        //被减数仅限于I、X、C    
    }    
};